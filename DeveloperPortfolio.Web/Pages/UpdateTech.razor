@page "/UpdateTech/{Id:int}"
@inherits UpdateTechBase

<h3>Update Technology</h3>
@if (TechDto == null && ErrorMessage == null)
{
    <LoadingSpinner />
}
else if (ErrorMessage != null)
{
    <DisplayError ErrorMessage="@ErrorMessage" />
}
else
{
    <EditForm Model="@TechDto" OnSubmit="@UpdateTech_Submit" class="card card-body bg-light mt-5">
        <div class="form-group row mb-1">
            <label for="name" class="col-md-2 col-form-label">Name:</label>
            <div class="col-md-10">
                <InputText id="name" class="form-control" @bind-Value="TechDto.Name" />
            </div>
        </div>
        <div class="form-group row mb-1">
            <label for="icon" class="col-md-2 col-form-label">Icon:</label>
            <div class="col-md-10">
                <InputText id="icon" class="form-control" @bind-Value="TechDto.Icon" />
            </div>
        </div>
        <div class="form-group row mb-1">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
    </EditForm>
}

